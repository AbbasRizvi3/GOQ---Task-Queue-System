services:
  postgres:
    image: postgres:15-alpine
    container_name: goq-postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
     - ./postgres_data:/var/lib/postgresql/data
     - ./migrations:/docker-entrypoint-initdb.d/
